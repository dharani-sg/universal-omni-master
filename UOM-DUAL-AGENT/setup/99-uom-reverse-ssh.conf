# UOM Dual-Agent: allow reverse tunnels from phone (Termux)
# Install: doas cp this file to /etc/ssh/sshd_config.d/ && doas rc-service sshd reload
AllowTcpForwarding yes
GatewayPorts no
AllowStreamLocalForwarding yes
